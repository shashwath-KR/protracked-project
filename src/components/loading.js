import React from "react";
import loading from '../img/loading.gif';

const Loading = () => {
    return (
        <div>
            <img  src={loading} alt= 'circle' width='110px' height= '110px' />
        </div>
    )
}

export default Loading;